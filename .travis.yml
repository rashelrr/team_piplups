language: python
python:
  - "3.6"      # current default Python on Travis CI
install:
  - pip install coverage
script:
  - coverage run -m unittest discover

#  - python -m unittest discover        # no module named 'requests'
#  - coverage run -m unittest discover # could not find coverage
